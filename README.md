##Command:
  
npx skills add halomoan/my-skills@nextjs-boilerplate --agent claude-code

##Note:  

The --agent claude-code flag is required — without it the installer scatters files into IDE adapter folders instead of ~/.agents/skills/.